const greeting = "Hello World";
const answer = 'Forty Two'

/* 
Third way of defining string is using backtick character.
Strings defined with the backtick character are called template strings
because they can be used as a template with dynamic values.
They support interpolation. One can inject any dynamic experssion 
in JavaScript within these dollar sign curly braces holders.

Use of multiple lines also is possible.
*/

const html = `<div>${Math.random()}</div>`;

